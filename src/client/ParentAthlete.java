package client;

import abstractClass.PersonAbstract;

public class ParentAthlete extends ClientBase {
    public ParentAthlete(String name, int age) {
        super.setName(name);
        super.setAge(age);
    }

}
